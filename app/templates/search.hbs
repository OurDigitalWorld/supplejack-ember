{{search-searchbar onChange=(action "updateParam" "text")}}
{{search-pagination meta=model.meta onChange=(action "updateOffset")}}
{{#each model as |item|}}
  <strong><a href={{item.source_url}}>{{item.title}}</a></strong> {{item.description}} <br />
{{/each}}