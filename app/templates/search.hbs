{{search-searchbar fields=recordFields param=text onChange=(action "updateParams")}}
{{search-pagination meta=model.meta onChange=(action "updateParams")}}
{{search-facets meta=model.meta param=and onChange=(action "updateParams")}}
{{#each model as |item|}}
  <strong><a href={{item.source_url}}>{{item.title}}</a></strong> {{item.description}} <br />
{{/each}}

