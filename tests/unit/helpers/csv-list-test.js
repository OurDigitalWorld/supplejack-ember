
import { csvList } from 'supplejack-client/helpers/csv-list';
import { module, test } from 'qunit';

module('Unit | Helper | csv list');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = csvList([42]);
  assert.ok(result);
});

