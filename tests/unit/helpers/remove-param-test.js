
import { removeParam } from 'supplejack-client/helpers/remove-param';
import { module, test } from 'qunit';

module('Unit | Helper | remove param');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = removeParam([42]);
  assert.ok(result);
});

