
import { mapPin } from 'supplejack-client/helpers/map-pin';
import { module, test } from 'qunit';

module('Unit | Helper | map pin');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mapPin([42]);
  assert.ok(result);
});

